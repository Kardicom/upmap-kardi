/**
 * Инструкция по работе с таблицами в TinaCMS
 * 
 * ВАЖНО: Встроенный rich-text редактор Tina НЕ поддерживает таблицы из коробки.
 * Tiptap Table расширения не добавляются автоматически в Tina editor.
 * 
 * Для полноценной работы с таблицами в Tina нужно:
 * 
 * 1. Использовать обычный Markdown синтаксис таблиц:
 *    | Заголовок 1 | Заголовок 2 |
 *    | ----------- | ----------- |
 *    | Ячейка 1    | Ячейка 2    |
 * 
 * 2. Или создать Custom Field для таблиц через ui.component
 * 
 * 3. Или переключить поле на type: "string" с ui.component: "textarea"
 *    и редактировать таблицы как сырой Markdown
 * 
 * Текущее решение: используй Markdown таблицы напрямую в редакторе.
 * Удаление строк/столбцов делается вручную в коде.
 */

export const TABLE_INSTRUCTIONS = `
Для редактирования таблиц в Tina:

1. Кликни на таблицу в редакторе
2. Переключись в режим "Raw" (если доступен)
3. Редактируй Markdown синтаксис:

| Столбец 1 | Столбец 2 |
|-----------|-----------|
| Данные 1  | Данные 2  |

Чтобы удалить:
- Строку: удали всю строку из Markdown
- Столбец: удали соответствующий столбец из всех строк
- Таблицу: выдели всю таблицу и нажми Delete
`;
